# Chef Chopsky Agent Service Environment Variables - Core Template
# This file contains only the essential variables needed across all environments
# For environment-specific configurations, see:
# - .env.local.example (local development)
# - .env.staging.example (staging environment)
# - .env.production.example (production environment)

# Required: OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# LangSmith Configuration (Optional but recommended)
LANGCHAIN_TRACING=true
LANGCHAIN_PROJECT=chef-chopsky-project
LANGCHAIN_API_KEY=your_langsmith_api_key_here

# Server Configuration
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# Frontend URL for CORS (Required for web integration)
FRONTEND_URL=http://localhost:3000

# Retriever & Embeddings Configuration
RETRIEVER_PROVIDER=memory
EMBEDDING_MODEL=openai/text-embedding-3-small

# Environment Discriminator
APP_ENV=local