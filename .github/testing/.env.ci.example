# Chef Chopsky CI Environment Variables
# This file combines both frontend and agent environment variables for CI testing
# Used by Act to run GitHub Actions workflows locally

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================

# Supabase Configuration (Required)
# CI uses local Supabase CLI instance
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
SUPABASE_SECRET_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# Agent Service Configuration (Required)
AGENT_SERVICE_URL=http://localhost:3001

# Environment Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=ci
APP_ENV=ci

# Development-specific settings
NEXT_PUBLIC_DEBUG_MODE=true
NEXT_PUBLIC_LOG_LEVEL=debug

# =============================================================================
# AGENT ENVIRONMENT VARIABLES
# =============================================================================

# Required: OpenAI API Key (Set your actual key here)
# Note: Replace with your actual OpenAI API key for real testing
OPENAI_API_KEY=sk-test-key-placeholder-replace-with-real-key

# LangSmith Configuration (Optional but recommended for CI)
LANGCHAIN_TRACING=true
LANGCHAIN_PROJECT=chef-chopsky-ci
LANGCHAIN_API_KEY=your_langsmith_api_key_here

# Server Configuration
PORT=3001
LOG_LEVEL=debug

# Frontend URL for CORS (Required for web integration)
FRONTEND_URL=http://localhost:3000

# LangGraph Configuration (if using LangGraph CLI)
LANGGRAPH_PORT=2024
LANGGRAPH_HOST=localhost

# Retriever & Embeddings Configuration (CI uses memory provider)
RETRIEVER_PROVIDER=memory
EMBEDDING_MODEL=openai/text-embedding-3-small

# Vector Store Configuration (for non-memory providers)
LANGCHAIN_INDEX_NAME=chef-chopsky
MONGO_NAMESPACE_PREFIX=retrieval

# Development-specific settings
ENABLE_MOCK_RESPONSES=false
DEBUG_MODE=true
ENABLE_DETAILED_LOGGING=true

# =============================================================================
# CI-SPECIFIC VARIABLES
# =============================================================================

# CI environment flag
CI=true

# Global CI environment (from ci.yml)
LOG_LEVEL=info
LANGGRAPH_PORT=2024
LANGGRAPH_HOST=localhost
LANGCHAIN_TRACING=true
LANGCHAIN_PROJECT=chef-chopsky-ci
